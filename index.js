
let meta = {
    value: 'ler um livro todo mês',
    checked: false,
    log: (info) => {
        console.log(info)
    }
}

meta.value = 'tocar piano'
meta.log(meta.value)

// functions  // arrow functions
const criarMeta = () => {}

// function criarMeta() {}